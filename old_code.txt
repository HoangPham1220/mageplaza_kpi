<script type="text/javascript">-->
<!--                    google.charts.load('current', {'packages': ['table']});-->
<!--                    google.charts.setOnLoadCallback(drawAgentTable);-->
<!---->
<!--                    /** Back url **/-->
<!--                    var search = window.location.href,-->
<!--                        agentId = search.search('agent');-->
<!--                    if (agentId !== -1) {-->
<!--                        search = search.substring(0, agentId - 1);-->
<!--                    }-->
<!--                    $('#agent-table-back').attr('href', search);-->
<!---->
<!--                    function drawAgentTable() {-->
<!--                        var detail = --><?php //= json_encode($surveyTicket->fetchTicketData()) ?>
<!--                       var rowData = Object.keys(detail).map(function (key) {
//                            let ticket = detail[key];
//                            let ticketUrl = '<a href="https://mageplaza.freshdesk.com/helpdesk/tickets/' + ticket['ticket_id'] + '" target="_blank">' + ticket['ticket_id'] + '</a>';
//                            let name = (agentId !== -1) ? ticket['extension_name'] : ticket['agent_name'];
//
//                            return [ticketUrl, ticket['ticket_status'], ticket['group_name'], ticket['subject'], name, ticket['ticket_type'], ticket['created_at'], ticket['updated_at']];
//                        });
//
//                        var options = {
//                            allowHtml: true,
//                            showRowNumber: true,
//                            width: '100%',
//                            height: '100%',
//                            sortAscending: true
//                        };
//                        var data = new google.visualization.DataTable();
//                        data.addColumn('string', 'Ticket ID');
//                        data.addColumn('string', 'Status');
//                        data.addColumn('string', 'Group');
//                        data.addColumn('string', 'Subject');
//                        data.addColumn('string', (agentId !== -1) ? 'Extension' : 'Agent');
//                        data.addColumn('string', 'Type');
//                        data.addColumn('string', 'Created');
//                        data.addColumn('string', 'Updated');
//                        data.addRows(rowData);
//                        var table = new google.visualization.Table(document.getElementById('agent-table'));
//                        table.draw(data, options);
//                        window.addEventListener('resize', function () {
//                            drawAgentTable();
//                        })
//                    }
                </script>